#!/usr/bin/make -f
# Pure Python, no build step: the upstream Makefile only installs.

export PYTHONDONTWRITEBYTECODE = 1

%:
	dh $@

override_dh_auto_install:
	$(MAKE) install PREFIX=/usr DESTDIR=debian/x7control

override_dh_auto_test:
	python3 -m pytest -q tests
